/**
 * @file Fixtures - spaces
 * @module fixtures/spaces
 */

import { chars } from '@flex-development/fsm-tokenizer'

/**
 * A string containing only spaces.
 *
 * @type {string}
 */
export default chars.space.repeat(3)

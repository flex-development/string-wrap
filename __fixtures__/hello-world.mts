/**
 * @file Fixtures - helloWorld
 * @module fixtures/helloWorld
 */

import { chars } from '@flex-development/fsm-tokenizer'

/**
 * A string containing several sequences and a line break.
 *
 * @type {string}
 */
export default `hello${chars.crlf}world 🌎`
